<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Only paints the window before Compose draws its first frame. Given an explicit background
         so that frame is the brand page colour rather than a flash of stock white. -->
    <style name="Theme.BirBookmarks" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>

    <color name="window_background">#F4F5F7</color>
</resources>
